package com.example.nd_mission.usedProducts.repo;

import org.springframework.data.jpa.repository.JpaRepository;

public interface usedProductRepository extends JpaRepository<usedProductRepository, Long> {
}
